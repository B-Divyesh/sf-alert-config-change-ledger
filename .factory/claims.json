[
  {
    "id": "core-workflow",
    "claim": "The bundled demo compares reviewed and live Grafana snapshots and attributes three route changes.",
    "where": "landing preview, demo, README",
    "test": "cargo test --test claims claim_core_workflow_no_license",
    "sandbox": "bundled sample exports in a temporary directory; no account or license"
  },
  {
    "id": "provider-inputs",
    "claim": "The CLI normalizes Grafana JSON and Alertmanager YAML exports.",
    "where": "landing how-it-works, README",
    "test": "cargo test --test claims claim_provider_inputs",
    "sandbox": "bundled Grafana and Alertmanager example exports"
  },
  {
    "id": "grafana-contact-points",
    "claim": "The CLI accepts Grafana contact-point arrays, detects PagerDuty, Opsgenie, Slack, webhook, and email recipient changes, and preserves non-secret provider fields and timestamps.",
    "where": "landing how-it-works, README",
    "test": "cargo test --test claims claim_grafana_contact_points_and_provider_fields",
    "sandbox": "paired bundled Grafana contact-point arrays with changed credentials and endpoints"
  },
  {
    "id": "read-only-import",
    "claim": "URL imports use GET and the CLI has no write command.",
    "where": "landing facts and boundaries, README",
    "test": "cargo test --test claims claim_read_only_get_and_command_surface",
    "sandbox": "temporary local HTTP listener and the compiled CLI help output"
  },
  {
    "id": "recipient-redaction",
    "claim": "Reports replace recipient endpoint values with SHA-256 fingerprints.",
    "where": "landing facts, privacy, README",
    "test": "cargo test --test claims claim_recipient_values_are_redacted",
    "sandbox": "bundled exports containing sample email and webhook endpoints"
  },
  {
    "id": "token-exclusion",
    "claim": "API tokens are sent as request authorization and are not written to snapshots.",
    "where": "README privacy",
    "test": "cargo test --test claims claim_api_tokens_never_enter_snapshots",
    "sandbox": "temporary local HTTP listener captures authorization; generated snapshot is searched for the token"
  },
  {
    "id": "exit-codes",
    "claim": "The CLI returns 0 without drift, 2 with drift, and 1 for command errors.",
    "where": "README usage",
    "test": "cargo test --test claims claim_documented_exit_codes",
    "sandbox": "bundled snapshots and malformed input in a temporary directory"
  },
  {
    "id": "free-core-cli",
    "claim": "Snapshot, diff, timeline, terminal, JSON, and Markdown workflows need no license.",
    "where": "landing facts and Pro section, README",
    "test": "cargo test --test claims claim_core_commands_and_formats_need_no_license",
    "sandbox": "compiled CLI with the license environment removed and temporary fixture outputs"
  },
  {
    "id": "no-telemetry",
    "claim": "The local demo runs with no telemetry or network dependency.",
    "where": "landing boundaries, README",
    "test": "cargo test --test claims claim_demo_needs_no_network_or_telemetry",
    "sandbox": "compiled demo with HTTP and HTTPS proxies pointed at a closed local port"
  },
  {
    "id": "demo-privacy",
    "claim": "The web demo makes only same-origin requests and uses a separate demo storage prefix.",
    "where": "demo banner, privacy, README",
    "test": "npm test -- --grep @claim:demo-privacy",
    "sandbox": "fresh browser context on /demo with every request and localStorage key recorded"
  },
  {
    "id": "demo-exit-clears-state",
    "claim": "Leaving the web demo removes every demo-namespaced browser storage item.",
    "where": "privacy, demo documentation",
    "test": "npm test -- --grep @claim:demo-exit-clears-state",
    "sandbox": "fresh browser context on /demo with an extra demo key and a real-mode key; activate Start for real and inspect localStorage"
  },
  {
    "id": "offline-reload",
    "claim": "The bundled demo reloads offline after the first visit.",
    "where": "landing facts, demo offline state",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; install service worker, reload once, then set the context offline"
  },
  {
    "id": "report-download",
    "claim": "The demo exports a JSON drift report with all three sample changes.",
    "where": "demo action",
    "test": "npm test -- --grep @claim:report-download",
    "sandbox": "fresh demo state; inspect the downloaded file and count change records"
  },
  {
    "id": "web-cli-parity",
    "claim": "The web demo reports the same sample comparison as the CLI.",
    "where": "landing preview",
    "test": "npm test -- --grep @claim:web-cli-parity",
    "sandbox": "fresh browser demo download compared with the compiled CLI demo JSON"
  },
  {
    "id": "paid-template",
    "claim": "Only a valid Pro license can retrieve the approval report pack.",
    "where": "landing Pro section, README",
    "test": "npm test -- --grep @claim:paid-template",
    "sandbox": "fresh browser context with recorded valid verification plus API tests for missing, invalid, and valid licenses"
  },
  {
    "id": "sales-closed",
    "claim": "New Pro license sales are not open in this release.",
    "where": "landing Pro section, README",
    "test": "npm test -- --grep @claim:sales-closed",
    "sandbox": "fresh browser context; assert the closed-sales notice and absence of checkout and paid-content links"
  }
]
